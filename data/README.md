# Data

- **`trader.sql`** — MySQL reference schema (Docker applies `mysql/init/` on first boot; keep both aligned).
- **`trader.sqlite`** — optional local SQLite reference (runtime DB defaults to `backend/instance/app.db`).
